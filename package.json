{
  "name": "adn-getter",
  "version": "1.0.0",
  "description": "Gets users and post data from ADN and mirrors them to local elasticsearch",
  "main": "getter.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "colors": "^1.1.2",
    "elasticsearch": "^5.0.0",
    "request": "^2.60.0"
  }
}
